interface:
  display_name: "Architecture Diagram Sync"
  short_description: "Maintain Mermaid architecture diagrams and review architecture drift."
  default_prompt: "Use this skill only for repository architecture diagram tasks: propose or revise Mermaid diagrams under docs/architecture, validate the supported Mermaid subset, and review diagram drift against code or infrastructure. Keep Mermaid as the canonical source of truth and treat Excalidraw as derived."

policy:
  allow_implicit_invocation: false
